#foreach($index in [1 .. ${maxLevel}])
<select id="J_region_select_$index" data-level="$index" data-fieldName="${fieldName}" data-tag="${fieldName}_${index}" class="region-select-field">
    <option value="" selected="selected">全部</option>
</select>
#end
<input type="hidden" id="J_id" name="${fieldId}" value="">
<input type="hidden" id="J_code" value="">
<input type="hidden" id="J_name" name="${fieldName}" value="">
<script type="text/javascript">
	$(function(){
		var sendData = {};
	    jQuery.ajax({
	        url: '$!{www}'+'/areainfo/list',
	        type: 'post',
	        dataType: 'json',
	        data: sendData,
	        success: function (back) {
	            
	        }
	    });
	});
</script>

