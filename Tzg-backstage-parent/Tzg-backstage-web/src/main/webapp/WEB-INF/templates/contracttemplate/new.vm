<div class="main-content">
    <div class="breadcrumbs" id="breadcrumbs">
        <script type="text/javascript">
            try{ace.settings.check('breadcrumbs' , 'fixed')}catch(e){}
            
        </script>

        <ul class="breadcrumb">
            <li>
                <i class="icon-home home-icon"></i>
                <a href="${www}/main">主页</a>
            </li>

            <li>
                <a href="${www}/contracttemplate">合同模板列表</a>
            </li>
            <li class="active">新增合同模板</li>
        </ul><!-- .breadcrumb -->

        <div class="nav-search" id="nav-search">
            <form class="form-search">
						<span class="input-icon">
							<input type="text" placeholder="Search ..." class="nav-search-input" id="nav-search-input" autocomplete="off" />
							<i class="icon-search nav-search-icon"></i>
						</span>
            </form>
        </div><!-- #nav-search -->
    </div>

    <div class="page-content">
        <div class="page-header">
            <h1>新增合同模板</h1>
        </div>
        
        #if(${msg})
        	<div>
			#if(${success})
        		<li class="text-success">$!{msg}</li>
   			 #else
        		<li class="text-danger">$!{msg}</li>
   			 #end    	
   			</div>
   		#end        
        
        <form id="inputForm" action="${www}/contracttemplate/save" method="post" class="form-horizontal">
        	<div class="row">
                <div class="col-xs-12 col-md-8">
                    <div class="form-group">
                         <label class="col-md-4 col-sm-2 control-label">模板名称：</label>
                        <div class="col-md-8 col-sm-8">
                            <input type="text" class="col-sm-12" id="vcName" name="vcName" value="$!{contracttemplate.vcName}"/>
                        </div>
                    </div>
                </div>
             </div>
             <div class="row">
	                <div class="col-xs-12 col-md-8">
	                    <div class="form-group">
	                       <label class="col-md-4 col-sm-2 control-label">模板路径：</label>
	                        <div class="col-md-8 col-sm-8">
	                           	<div class="row">
		                        	<div class="col-md-11">
		                        		<input type="text" class="col-sm-12" id="contracttemplatePath" value="$!{contracttemplate.vcURL}"  readonly/>
		                        		<input type="hidden" id="vcURL" name="vcURL" value="$!{contracttemplate.vcURL}">
		                        	</div>
		                        	<div class="col-md-1">
		                        		<!--<button type="button" id="select_contracttemplate_path_btn" class="btn btn-xs">选择模板路径</button>-->
										<input type="file" id="file_upload" value="选择模板路径"/>
		                        	</div>
		                        </div>	
	                        </div>
	                    </div>
	                </div>
	          </div>
            <div class="clearfix form-actions">
                <div class="row">
                    <div class="col-md-8">
                        <div class="col-sm-offset-4 col-md-9">
           					<button class="btn btn-info" type="submit">
                                <i class="icon-ok bigger-110"></i>提交
                            </button>
                            &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                            <button class="btn" type="button" onclick="javascript:history.go(-1);">
                                <i class="icon-arrow-left bigger-110"></i>返回
                            </button>
                        </div>
                    </div>
                </div>
            </div>
     </div>
    </form>
</div><!-- /.page-content -->
</div><!-- /.main-content -->

<script src="${www}/js/sea.js" id="seajsnode"></script>
<script type="text/javascript" src="${www}/js/uploadify/jquery.uploadify.min.js"></script>
<script type="text/javascript">

$(function(){
	$("#file_upload").uploadify({
		 method:'post',
        'swf'      : '${www}/js/uploadify/uploadify.swf',
        'uploader' : '${www}/projectrisk/upload;jsessionid=$!session.getId()',
         buttonText: '<button>选择文件</button>',
		 fileObjName:'file',
		 fileSizeLimit:'10MB',
		 fileTypeExts: '*.vm',
		 width: 120,
		 height: 30,
		 multi: false,
		 progressData: 'speed',
		 onCancel: function(file){
                        console.log('The file'+ file.name + 'was cancelled.')
                },
		 onSelect: function(file){
                    console.log(file.name)
                },
		onUploadComplete: function(file){
                    //在每一个文件上传成功或失败之后触发，返回上传的文件对象或返回一个错误，如果你想知道上传是否成功，最后使用onUploadSuccess或onUploadError事件
					 
                },
		onUploadError: function(file,errorCode,erorMsg,errorString){
                },
		onUploadStart: function(file){
				 
                },
		onSWFReady: function(){
                  
                },
		onUploadSuccess: function(file,data,response){
					var vcURL = $("#vcURL");
					 var contracttemplatePath = $("#contracttemplatePath");
					var d = eval('(' + data + ')');
				 	vcURL.val(d.url);
                    contracttemplatePath.val(d.url);
                }
		
    });
});
/*
seajs.use(['dialog'], function(dialog) {
    var vcURL = $("#vcURL");
    var contracttemplatePath = $("#contracttemplatePath");
    var showDialog = function () {
        dialog.alert({
            title: '合同模板列表',
            content: '<iframe id="js_selector" frameborder="0" width="800" height="400" src="${www}/uploadfile/filePathList?itype=1"></iframe>',
            ok: function() {
                var checkedRadio = $(":radio:checked", document.getElementById("js_selector").contentDocument);
                if (checkedRadio.size()) {
                    vcURL.val(checkedRadio.val());
                    contracttemplatePath.val(checkedRadio.attr("data-name"));
                }
            }
        });
    };
    contracttemplatePath.click(showDialog);
    $("#select_contracttemplate_path_btn").click(showDialog);
});
*/
</script>
